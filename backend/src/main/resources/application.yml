spring:
  application:
    name: dealership
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/dealership}
  security:
    user:
      name: user
      password: password
